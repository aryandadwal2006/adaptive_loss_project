# Baseline training configurations
- name: baseline_default
  training:
    num_epochs: 50
    batch_size: 32
    learning_rate: 0.001
    weight_decay: 0.0001
  model:
    type: SimpleCNN
    hidden_dim: 64
  optimizer: SGD
